# Figure Sources

The Python script that draws each of the book's **74 figures**, so a figure can be regenerated, corrected, or adapted for slides. These already exist; they need only to be published — one directory per chapter, with file names matching the figure numbers.

!!! warning "In preparation"
    The figure-source scripts will be published here, organized one folder per chapter (`figures/ch01/`, `figures/ch02/`, …) with each file named for the figure it draws (e.g. `figure_2_4.py`). Each will regenerate its figure from data, so it can be corrected or restyled for a talk.

**Source needed:** the figure-drawing scripts (the code behind the 74 figures). Once you share them, I'll publish them per chapter and link each to its figure number.
